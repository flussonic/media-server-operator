---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: mediaservers.media.flussonic.com
spec:
  group: media.flussonic.com
  names:
    kind: MediaServer
    listKind: MediaServerList
    plural: mediaservers
    singular: mediaserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MediaServer is the Schema for the mediaservers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MediaServerSpec defines the desired state of MediaServer
            properties:
              foo:
                description: Foo is an example field of MediaServer. Edit mediaserver_types.go
                  to remove/update
                type: string
            type: object
          status:
            description: MediaServerStatus defines the observed state of MediaServer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
